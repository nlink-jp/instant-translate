# instant-translate

macOS 標準の **Translation framework**（オンデバイス翻訳）を使う**軽量**な
メニューバー翻訳アプリ。メニューバーからパネルを開き、翻訳して、コピー —
数十GBのモデルをロードする必要はありません。

[`quick-translate`](https://github.com/nlink-jp/quick-translate)（ローカル LLM）の
**軽量姉妹**です。メニューバーの UX は同じで、バックエンドが違います。LLM の
品質・カスタム性が欲しいときは `quick-translate`、素早く軽く使いたいときは
instant-translate、と使い分けます。macOS 26 以降（Apple silicon）。Developer ID
署名 + notarize 済み。

> **ステータス: リリース前（Phase 1 scaffold）。**

## できること

- **メニューバー**: クリックで翻訳パネルを開く。
- **入力/貼付** した原文は、入力が止まって少し経つと**自動翻訳**（設定でON/OFF）。
  すぐ翻訳したいときは ⌘↩。訳文は **コピー** して再利用。
  自動翻訳は IME を待ちます。かな漢字変換など**変換が確定していない間は翻訳しません**ので、
  変換途中の文字列が翻訳されたり、言語選択ダイアログに邪魔されたりしません。
- **賢い言語ルーティング**: 入力言語は自動認識。出力は既定でシステムの言語へ。
  *自分の言語* で入力した場合は、出力を設定した **二次言語**（例: 英語）へ自動で
  切り替え（設定で ON/OFF）。つまり *外国語 → 自国語 / 自国語 → 設定した外国語*。
- **訳先の手動選択**: パネルのピッカーで、Auto の代わりに一時的に特定の言語へ翻訳できます
  （アプリ再起動で Auto に戻ります）。
- **グローバルホットキー**: **⌥⌘T**（設定で変更可）でどこからでもパネルを開く。
  開いたときにクリップボードの内容を原文に流し込んで自動翻訳することもできます。
- **OS 対応言語のみ**: ピッカーには Mac の Translation framework が実際に対応する言語だけが
  並びます。未対応のペアは分かりやすく通知します。
- **ログイン時に起動**: 任意（設定 → General）。
- **揮発する履歴**: 直前の翻訳のみをメモリ上に保持（ディスクに書きません）。
  終了で消えます。

**特別な権限は不要:** Accessibility などの TCC 権限は要りません（OS 側の言語モデル
ダウンロード同意のみ）。

## 仕組み

翻訳は macOS の Translation framework によって **完全にオンデバイス** で行われます。
言語ペアを初めて使うときに、macOS が（軽量な OS 管理の）モデルを同意のうえ
ダウンロードします。アプリ自体は **ネットワーク・API キー・クレデンシャル一切不要**。

## ビルド

```sh
make run          # ビルド + 実行（デバッグ）
make build        # リリースバイナリ → .build/release/
make build-app    # 署名済み .app → dist/
make package      # build-app + notarize + staple + zip（リリース）
make test
```

macOS 26 SDK（新しめの Xcode / Command Line Tools）が必要です。

## なぜ Apple Translation（LLM ではなく）か

`quick-translate` は高品質・カスタム可能な翻訳のためにローカル LLM（数十GB）を
ロードしますが、起動が遅くメモリ負荷も大きい。instant-translate はそれを OS の
オンデバイスモデルに置き換え、ほぼ瞬時・軽量で、日常の短文翻訳には十分な品質を
得ます。両者は共存します。

## ライセンス

MIT — [LICENSE](LICENSE) を参照。
