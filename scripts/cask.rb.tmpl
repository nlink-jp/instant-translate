cask "@NAME@" do
  version "@VERSION@"
  sha256 "@SHA256@"

  url "https://github.com/nlink-jp/@REPO@/releases/download/v#{version}/@NAME@-v#{version}-darwin-arm64.zip"
  name "@NAME@"
  desc "@DESC@"
  homepage "https://github.com/nlink-jp/@REPO@"

  # Developer ID signed + Apple-notarized + stapled .app (Apple Silicon only).
  # :tahoe (macOS 26), NOT the shared template's :big_sur floor — the app requires
  # the macOS 26 Translation API (LSMinimumSystemVersion=26.0). The symbol alone
  # means ">= that version" (the '">= :tahoe"' string form is deprecated).
  depends_on arch: :arm64
  depends_on macos: :tahoe

  app "@APP@"

  zap trash: [
    "~/Library/Caches/@BUNDLE_ID@",
    "~/Library/Preferences/@BUNDLE_ID@.plist",
    "~/Library/Saved Application State/@BUNDLE_ID@.savedState",
    "~/Library/WebKit/@BUNDLE_ID@",
  ]
end
